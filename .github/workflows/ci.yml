name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  secrets-scan:
    name: Secrets scan (gitleaks)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install gitleaks
        run: |
          set -e
          VERSION=8.21.2
          curl -sSL "https://github.com/gitleaks/gitleaks/releases/download/v${VERSION}/gitleaks_${VERSION}_linux_x64.tar.gz" \
            | sudo tar -xz -C /usr/local/bin gitleaks
          gitleaks version
      - name: Run gitleaks
        run: gitleaks detect --source . --config .gitleaks.toml --redact --verbose --no-banner
      - name: Block tfstate / tfvars / SA JSON from being committed
        run: |
          set -e
          bad=$(git ls-files | grep -E '(\.tfstate(\.backup)?$|(^|/)terraform\.tfvars$|(^|/)firebase-debug\.log$|(^|/)[^/]*-sa\.json$|(^|/)service-account.*\.json$|(^|/)gcp-[^/]*-key\.json$)' || true)
          if [ -n "$bad" ]; then
            echo "ERROR: These files must never be tracked in git:"
            echo "$bad"
            exit 1
          fi

  backend-lint-test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: test_koinoflow
          POSTGRES_USER: koinoflow
          POSTGRES_PASSWORD: koinoflow
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r backend/requirements.txt
      - run: ruff check backend/
      - run: ruff format --check backend/
      - run: pytest backend/ --tb=short
        env:
          DJANGO_SETTINGS_MODULE: config.settings.local
          DATABASE_URL: postgres://koinoflow:koinoflow@localhost:5432/test_koinoflow
          REDIS_URL: redis://localhost:6379/0
          SECRET_KEY: ci-test-secret-key

  frontend-lint-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
        working-directory: frontend
      - run: npx eslint src/
        working-directory: frontend
      - run: npx tsc --noEmit
        working-directory: frontend
      - run: npx vitest run --passWithNoTests
        working-directory: frontend

  mcp-server-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r mcp-server/requirements.txt -r mcp-server/requirements-dev.txt
      - run: ruff check mcp-server/
      - run: ruff format --check mcp-server/
